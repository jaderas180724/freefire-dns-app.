{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" } }
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list" } },
      "targets": [
        {
          "expr": "rate(proxy_requests_total[5m])",
          "legendFormat": "{{method}} - modified={{modified}}"
        }
      ],
      "title": "Proxy Request Rate",
      "type": "timeseries"
    },
    {
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" } }
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, rate(proxy_request_duration_seconds_bucket[5m]))",
          "legendFormat": "p95"
        },
        {
          "expr": "histogram_quantile(0.50, rate(proxy_request_duration_seconds_bucket[5m]))",
          "legendFormat": "p50"
        }
      ],
      "title": "Proxy Request Latency",
      "type": "timeseries"
    },
    {
      "fieldConfig": { "defaults": {} },
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        { "expr": "proxy_modified_responses_total" }
      ],
      "title": "Total Modified Responses",
      "type": "stat"
    },
    {
      "fieldConfig": { "defaults": {} },
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 8 },
      "id": 4,
      "targets": [
        { "expr": "sum(rate(proxy_requests_total[1m]))" }
      ],
      "title": "Current RPS",
      "type": "stat"
    },
    {
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" } }
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 12 },
      "id": 5,
      "targets": [
        {
          "expr": "rate(http_requests_total[5m])",
          "legendFormat": "{{method}} {{route}} - {{status_code}}"
        }
      ],
      "title": "Config Service Request Rate",
      "type": "timeseries"
    },
    {
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" } }
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 12 },
      "id": 6,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))",
          "legendFormat": "p95 {{route}}"
        }
      ],
      "title": "Config Service Latency",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["devflow-labs", "proxy"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "DevFlow Labs - Proxy Interceptor",
  "uid": "devflow-proxy",
  "version": 1
}
